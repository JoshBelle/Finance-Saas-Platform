import {
    Cell,
    Legend,
    Pie,
    PieChart,
    ResponsiveContainer,
    Tooltip
} from 'recharts'

const COLORS = ['#0062ff', '#12c6ff', '#ff647f', '#ff9354']

type Props = {
    data: {
        name: string,
        value: number,
    }[]
}

export const PieVariant = ({ data }: Props) => {
    return (
        <Res></Res>
    )
}